{
    admin off
    email contact@earthfast.com
}

{$SERVER_NAME} {
    tls {
        issuer acme
    }

    handle {
        reverse_proxy content-node:5000
    }

    handle /statusz {
        reverse_proxy content-node:5000
    }

    log {
        output stdout
        format console
    }
}
